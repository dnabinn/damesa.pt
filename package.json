{
  "name": "mesa-pt",
  "version": "1.0.0",
  "description": "Mesa.pt — Restaurant booking platform for Lisbon. Powered by RestaurantOS.",
  "scripts": {
    "dev": "npx serve pages -p 3000",
    "deploy:functions": "supabase functions deploy send-notification && supabase functions deploy send-reminders",
    "db:reset": "supabase db reset",
    "db:seed": "supabase db execute --file sql/seed_restaurants.sql",
    "db:push": "supabase db push"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Nabin — RestaurantOS",
  "license": "UNLICENSED",
  "private": true
}
